<div class="wrapper-uers-index">
  <div class="container">
    <h1>ユーザー一覧<h1>
      <% @users.each do |user| %>
        <div>
          <p>ユーザーID: <span><%= user.id %></p>
          <%= link_to "#{user.name}", user_path(id: user.id) %>
      </div>
      <% end %>


  </div>
</div>
